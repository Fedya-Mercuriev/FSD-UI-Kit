doctype html
html(lang="en")
    head
        meta(charset="utf-8")
        title="CV"
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet" href="<%=htmlWebpackPlugin.files.chunks.common.css %>")
    body
        script(src="<%= htmlWebpackPlugin.files.chunks.main.entry %>")